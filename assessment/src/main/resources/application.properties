spring.application.name=assessment
server.port=8083

# ?? Microservice URLs (Docker service names, NOT localhost) ??????????????????
demographics.service.url=${DEMOGRAPHICS_URL:http://demographics:8081}
notes.service.url=${NOTES_URL:http://notes:8082}

# ?? Actuator ?????????????????????????????????????????????????????????????????
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
