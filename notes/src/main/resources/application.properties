spring.application.name=notes
server.port=8082

# MongoDB ? Spring Boot 4 requires the full URI; individual host/port/credential properties are ignored
spring.data.mongodb.uri=mongodb://medilabo:medilabo123@mongodb:27017/notes_db?authSource=admin

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
